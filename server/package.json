{
  "name": "fmd-api",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "FMD backend — serves [List] entities from SQL tables and [Store] entities from JSONB collections (single Postgres).",
  "scripts": {
    "start": "node index.js"
  },
  "dependencies": {
    "express": "^4.19.2",
    "jose": "^5.9.6",
    "pg": "^8.11.5"
  }
}
